@{
    ViewBag.Title = "Bem vindo ao site da ACervA Carioca!";
}
<h3>@ViewBag.Title</h3>

<div ng-repeat="noticia in listaCtrl.listaNoticias">
    <h4 class="text-primary">{{noticia.titulo}}</h4>
    <span ng-bind-html="noticia.textoHtml"></span>
</div>