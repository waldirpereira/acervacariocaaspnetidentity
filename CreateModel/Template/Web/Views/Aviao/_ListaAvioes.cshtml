<div class="panel panel-primary">
    <div class="panel-heading">Aviões</div>
    <div class="panel-body">
        <div class="row hidden-xs">
            <table id="tabelaAvioes"
                   datatable="ng" dt-disable-deep-watchers="true"
                   dt-multifiltering="multifiltering" class="table table-bordered table-striped table-responsive table-hover table-condensed">
                <thead>
                <tr>
                    <th style="width: 60px;">Código</th>
                    <th>Nome</th>
                    <th style="width: 40px;">Ativ%ARTIGO%?</th>
                    <th class="sorting_disabled" style="width: 60px;"></th>
                </tr>
                </thead>
                <tbody>
                <tr ng-repeat="m in ctrl.listaAvioes track by m.codigo">
                    <td>
                        <input type="hidden" value="{{('0000000000'+m.codigo).slice(-10)}}"/>
                        <a title="Editar avião"
                           ng-href="#/Edit/{{m.codigo}}">{{m.codigo}}</a>
                    </td>
                    <td>
                        <a title="Editar avião"
                           ng-href="#/Edit/{{m.codigo}}">{{m.nome}}</a>
                    </td>
                    <td>
                        <input type="hidden" ng-value="!m.ativo"/>
                        <span class="label label-success" ng-if="m.ativo">Sim</span>
                        <span class="label label-danger" ng-if="!m.ativo">Não</span>
                    </td>

                    <td style="padding: 4px;">
                        <a class="btn btn-info btn-xs" title="Editar {{ m.nome }}"
                           ng-if="m.ativo"
                           href="#/Edit/{{m.codigo}}">
                            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                        </a>
                        <a class="btn btn-default btn-xs" title="Visualizar {{ m.nome }}"
                           ng-if="!m.ativo"
                           href="#/Edit/{{m.codigo}}">
                            <span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span>
                        </a>
                        <button type="button" class="btn btn-success btn-xs" title="Ativar {{ m.nome }}"
                                acerva-confirm
                                ng-if="!m.ativo"
                                confirm-action="ctrl.ativa(m)"
                                confirm-window-type="success"
                                confirm-text="Confirma a ativação d%ARTIGO% avião {{ m.nome }}?">
                            <span class="glyphicon glyphicon-ok" aria-hidden="true"></span>
                        </button>
                        <button type="button" class="btn btn-danger btn-xs" title="Inativar {{ m.nome }}"
                                acerva-confirm
                                ng-if="m.ativo"
                                confirm-action="ctrl.inativa(m)"
                                confirm-window-type="danger"
                                confirm-text="Confirma a inativação d%ARTIGO% avião {{ m.nome }}?">
                            <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
                        </button>
                    </td>
                </tr>
                </tbody>
            </table>
        </div>
        <div class="row visible-xs">
            <table class="table table-bordered table-striped table-responsive table-hover table-condensed visible-xs" style="width: 100%;">
                <thead>
                <tr>
                    <th>Avião</th>
                </tr>
                </thead>
                <tbody>
                <tr ng-repeat="m in ctrl.listaAvioes track by m.codigo" style="margin-bottom: 5px;">
                    <td>
                        <strong>
                            <a title="Editar avião"
                               ng-href="#/Edit/{{m.codigo}}">{{m.nome}}</a>
                        </strong>
                        <span>
                            <span class="label label-success pull-right" ng-if="m.ativo">ativ%ARTIGO%</span>
                            <span class="label label-danger pull-right" ng-if="!m.ativo">inativ%ARTIGO%</span>
                        </span>
                        <br />
                        <a class="btn btn-info btn-xs" title="Editar {{ m.nome }}"
                           ng-if="m.ativo"
                           href="#/Edit/{{m.codigo}}">
                            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                        </a>
                        <a class="btn btn-default btn-xs" title="Visualizar {{ m.nome }}"
                           ng-if="!m.ativo"
                           href="#/Edit/{{m.codigo}}">
                            <span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span>
                        </a>
                        <button type="button" class="btn btn-success btn-xs" title="Ativar {{ m.nome }}"
                                acerva-confirm
                                ng-if="!m.ativo"
                                confirm-action="ctrl.ativa(m)"
                                confirm-window-type="success"
                                confirm-text="Confirma a ativação d%ARTIGO% avião {{ m.nome }}?">
                            <span class="glyphicon glyphicon-ok" aria-hidden="true"></span>
                        </button>
                        <button type="button" class="btn btn-danger btn-xs" title="Inativar {{ m.nome }}"
                                acerva-confirm
                                ng-if="m.ativo"
                                confirm-action="ctrl.inativa(m)"
                                confirm-window-type="danger"
                                confirm-text="Confirma a inativação d%ARTIGO% avião {{ m.nome }}?">
                            <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
                        </button>
                    </td>
                </tr>
            </table>
        </div>
    </div>
    <div class="panel-footer">
        <div class="row">
            <div class="col-sm-12">
                <div class="pull-right">
                    <a class="btn btn-primary" href="#/Create">Nov%ARTIGO% avião</a>
                </div>
            </div>
        </div>
    </div>
</div>