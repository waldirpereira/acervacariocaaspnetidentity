<div class="modal-header alert-primary">
    <h3 class="modal-title" id="modal-title">E-mails a serem selecionados</h3>
</div>
<div class="modal-body" id="modal-body">
    <div class="row">
        <div class="col-sm-12">
            <form name="formSelecaoEmails" novalidate>
                <div class="row">
                    <div class="col-sm-12">
                        <label class="control-label" for="emails">E-mails (entre vírgulas ou espaços ou quebras de linha ou tabs)</label>
                        <textarea id="emails" ng-model="ctrl.modelo.emails" class="form-control"></textarea>
                        <p ng-show="formSelecaoEmails.emails.$invalid && !formSelecaoEmails.emails.$pristine" class="help-block">O campo de e-mails é obrigatório</p>
                    </div>
                </div>
                <div class="row">
                    <div class="col-sm-12">
                        <input type="checkbox" ng-model="ctrl.modelo.limpaSelecaoAtual"/> Limpar seleção atual
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
<div class="modal-footer">
    <button class="btn btn-primary" type="button" ng-click="ctrl.ok()">OK</button>
</div>